frontend:
    path: ./                   # Path to your static files (root directory in this case)
    publish: ""                # Leave empty for static sites
    scripts:
        build: ""              # No build script needed for static HTML
        deploy: ""             # No deploy script needed for static HTML
    subdomain: info-sphere      # Custom subdomain for your site
name: info-sphere
region: us-east-1              # Deployment region
yamlVersion: 2                 # YAML version
